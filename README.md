# Telegram bot for searching books

## How to run

install poetry
```
pipx install poetry
```
go to main directory and run it

```
poetry install
```

before executing main.py create .env file in src directory with it inside
```
TOKEN = "Your telegram API token"
```
